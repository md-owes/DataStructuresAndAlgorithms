#pragma once
#ifndef BubbleSort_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define BubbleSort_H

//inline void swap(int *xp, int *yp);
//int bubble(int arr[], int i);
void bubbleSort(int arr[], int n);

#endif