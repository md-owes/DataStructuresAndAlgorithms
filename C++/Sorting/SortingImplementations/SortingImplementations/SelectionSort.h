#pragma once
#ifndef SelectionSort_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define SelectionSort_H

//int select(int arr[], int n);
void selectionSort(int arr[], int n);
#endif